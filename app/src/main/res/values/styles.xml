<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <!--    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">-->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryDark">@color/light_green</item>
        <item name="colorAccent">@color/green</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="colorPrimary">@android:color/transparent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TextAppearance.Design.Snackbar.Message" parent="android:TextAppearance" tools:override="true">
        <item name="android:textColor">@color/white</item>
        <!--        <item name="android:textSize">50sp</item>-->
    </style>

    <!--    <style name="TabLayoutStyle" parent="Widget.Design.TabLayout">-->
    <!--    <style name="TabLayoutStyle" parent="Widget.MaterialComponents.TabLayout.Colored">-->
    <style name="TabLayoutStyle" parent="Widget.MaterialComponents.TabLayout">
        <!--        Cannot use drawables here, colors only.-->
        <item name="android:background">@color/background_tab_layout</item>
        <item name="tabTextAppearance">@style/TabTextAppearance</item>
        <item name="tabTextColor">@color/grey_darkest</item>
        <item name="tabSelectedTextColor">@color/darker_green</item>
        <item name="tabIndicatorHeight">1dp</item>
        <item name="tabPaddingStart">1dp</item>
        <item name="tabPaddingEnd">1dp</item>
        <item name="tabGravity">fill</item>
        <item name="tabIconTint">@color/selector_tab_icon_tint</item>
        <!--        <item name="tabMinWidth">78dp</item>-->
        <item name="tabMode">scrollable</item>
        <!--        <item name="tabIndicatorColor">@color/darker_green</item>-->
    </style>

    <style name="TabTextAppearance" parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="android:textAllCaps">false</item>
        <!--        <item name="android:textSize">16sp</item>-->
    </style>

</resources>
